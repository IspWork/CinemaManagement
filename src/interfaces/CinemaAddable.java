package interfaces;

/**
 * Interface that allows for a cinema to be added to a collection.
 *
 */
public interface CinemaAddable {
	/**
	 * Adds a cinema to a collection.
	 * 
	 * @param cinema
	 *            - the desired cinema to be added.
	 */
	void addCinema(Cinema cinema);

}
