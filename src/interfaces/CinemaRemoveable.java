package interfaces;

/**
 * Interface that allows for a cinema to be removed.
 *
 */
public interface CinemaRemoveable {
	/**
	 * Removes a certain cinema.
	 * 
	 * @param cinema
	 *            - the desired cinema to be removed.
	 */
	void removeCinema(Cinema cinema);

}
