package interfaces;

/**
 * Interface that enables the adding of movies.
 *
 */
public interface MovieAddable {
	/**
	 * Method that adds a movie.
	 * 
	 * @param movie
	 *            - the movie to be added
	 */
	void addMovie(Movie movie);
}
